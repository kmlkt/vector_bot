services:
  app:
    image: ghcr.io/kmlkt/vector_bot:${APP_VERSION}
    restart: unless-stopped
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
    volumes:
      - ./storage:/app/storage
    ports:
      - "8080:8080"
